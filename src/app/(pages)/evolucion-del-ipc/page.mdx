import IPCCurrentPeriod from '@/components/charts/IPCCurrentPeriod'
import IPCByProductType from '@/components/charts/IPCByProductType'

export const metadata = {
  title: 'Evolución del IPC'
}

# Evolución del IPC

Evolución del Índice de precios al consumo (IPC) en La Rioja, el valor corresponde a la variación interanual del IPC en el último mes con respecto al mismo mes del año anterior y se muestra por grupos de consumo ECOICOP.

<IPCCurrentPeriod originUrl="https://web.larioja.org/dato-abierto/datoabierto?n=opd-57" />

## Evolución del IPC por producto

Evolución del Índice de precios al consumo (IPC) en La Rioja, el valor corresponde a la variación interanual del IPC en el último mes con respecto al mismo mes del año anterior, se muestran agrupados por tipo de producto.

<IPCByProductType originUrl="https://web.larioja.org/dato-abierto/datoabierto?n=opd-124" />
